# Hoist packages so node_modules/.bin is correctly populated
shamefully-hoist=true
# Install devDependencies even when NODE_ENV=production
# (needed for TypeScript types; runtime uses dependencies only)
ignore-scripts=false
